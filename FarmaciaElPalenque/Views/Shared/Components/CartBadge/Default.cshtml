@model int

<a asp-controller="Carrito" asp-action="Ver" class="cart-fab" aria-label="Ir al carrito">
    <i class="bi bi-cart3"></i>
    @if (Model > 0)
    {
        <span class="cart-fab-badge">@Model</span>
    }
</a>